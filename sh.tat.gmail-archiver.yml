app-id: 'sh.tat.gmail-archiver'
command: 'gmail-archiver'
finish-args:
  - '--share=ipc'
  - '--share=network'
  - '--socket=fallback-x11'
  - '--socket=wayland'
modules:
  - build-commands:
      - 'pip3 install --prefix=/app uv'
      - '/app/bin/uv pip install --prefix=/app .'
    build-options:
      build-args:
        - '--share=network'
    buildsystem: 'simple'
    name: 'gmail-archiver'
    sources:
      - tag: 'v0.1.1'
        type: 'git'
        url: 'https://github.com/Tatsh/gmail-archiver'
runtime: 'org.freedesktop.Platform'
runtime-version: '24.08'
sdk: 'org.freedesktop.Sdk'
